name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 72.61.227.248
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          timeout: 60s
          script: |
            cd /var/www/doonmeet
            git pull origin main
            /home/ravish/.nvm/versions/node/v24.18.0/bin/npm install
            /home/ravish/.nvm/versions/node/v24.18.0/bin/npm run build
            /home/ravish/.nvm/versions/node/v24.18.0/bin/pm2 restart doonmeet1~script: |
            cd /var/www/doonmeet
            git pull origin main
            /home/ravish/.nvm/versions/node/v24.18.0/bin/npm install
            /home/ravish/.nvm/versions/node/v24.18.0/bin/npm run build
            /home/ravish/.nvm/versions/node/v24.18.0/bin/pm2 restart doonmeet
